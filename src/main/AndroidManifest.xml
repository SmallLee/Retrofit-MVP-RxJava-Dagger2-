<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hecun.rxproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.view.activity.SecondActivity" />
        <activity android:name=".mvp.view.activity.SchedulersActivity" />
        <activity android:name=".mvp.view.activity.SubjectActivity" />
        <activity android:name=".mvp.view.activity.DemoActivity" />
        <activity android:name=".base.BaseActivity" />
        <activity android:name=".mvp.view.activity.EmptyActivity"></activity>
    </application>

</manifest>